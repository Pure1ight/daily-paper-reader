---
title: "Multi-Modal Learning meets Genetic Programming: Analyzing Alignment in Latent Space Optimization"
title_zh: 多模态学习与遗传规划的结合：分析潜空间优化中的对齐
authors: "Benjamin Léger, Kazem Meidani, Christian Gagné"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08324v2"
tags: ["query:pde-discovery"]
score: 8.0
evidence: 使用遗传规划和潜空间优化的符号回归
tldr: 本研究探讨了多模态学习在符号回归中的应用，特别是针对SNIP模型。SNIP试图通过对比学习对齐符号和数值编码器，将组合搜索转化为连续空间优化。然而，实验发现SNIP的学习对齐过于粗糙，无法在符号空间进行高效搜索，且对齐程度在优化过程中并未提升。研究指出了实现细粒度对齐是未来多模态潜在空间优化的关键方向。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.08324v2/fig-001.webp\", \"caption\": \"Figure 4: Sensitivity analysis excluding constant change perturbations on Feynman. (a) Accuracy comparison showing that even without constant changes, model accuracy (23.2%) remains below random baseline (32.1%). (b) Rank distribution comparison showing improved but still suboptimal performance when constant changes are excluded.\", \"page\": 7, \"index\": 1, \"width\": 943, \"height\": 308}, {\"url\": \"assets/figures/arxiv/2604.08324v2/fig-002.webp\", \"caption\": \"Figure 1: Overview of Latent Space Optimization and multi-modal alignment. a) Traditional LSO-GP framework : a continuous optimizer searches the latent space of a symbolic autoencoder. b) SNIP’s [19] contrastive pre-training: symbolic and numerical encoders are trained to align embeddings of matched expression pairs with bat. c) Illustration of alignment granularity limitations : given a base expression 𝑓 ★ = sin(𝑥 + 1) and its numeric embedding 𝑍★ 𝑉 (labeled 𝑁★ in the figure) encoded from observations (𝑋,𝑦★), the model should assign highest similarity between 𝑍★ 𝑉 and the symbolic embedding 𝑍★ 𝑆 (labeled 𝑆★) over embeddings of perturbed variants (𝑆1, 𝑆2, 𝑆3). In practice, the learned alignment often fails to make this distinction.\", \"page\": 3, \"index\": 2, \"width\": 1036, \"height\": 269}, {\"url\": \"assets/figures/arxiv/2604.08324v2/fig-003.webp\", \"caption\": \"Figure 2: Evolution of (a) 𝑅2 fitness (top) and (b) cross-modal alignment (bottom) during LSO across all Feynman and Strogatz equations, averaged over all equations. While fitness improves consistently, alignment remains flat or decreases, indicating that optimization does not exploit cross-modal alignment to produce better symbolic solutions. Iteration 0 corresponds to the model’s one-shot prediction, iteration 1 to the best individual after initialization and BFGS constant refinement.\", \"page\": 4, \"index\": 3, \"width\": 496, \"height\": 715}, {\"url\": \"assets/figures/arxiv/2604.08324v2/fig-004.webp\", \"caption\": \"Figure 3: Distribution of ranks for the correct expression across 82 test cases of Feynman set. The correct expression most frequently ranks second (39 cases), with only 15 cases achieving rank 1.\", \"page\": 5, \"index\": 4, \"width\": 443, \"height\": 303}]"
motivation: 验证基于对比学习的多模态潜在空间优化方法是否能真正实现符号与数值模态的细粒度对齐，以有效引导符号回归的搜索过程。
method: 通过实验分析SNIP模型在优化过程中的跨模态对齐动态变化，评估其对齐质量对符号空间搜索的实际引导作用。
result: 实验发现跨模态对齐在优化过程中并未随适应度增加而改善，且现有对齐粒度过粗，无法支持高效的符号空间搜索。
conclusion: 目前的多模态潜在空间优化尚未实现有效的对齐引导，未来研究应聚焦于提升符号与数值模态之间的细粒度对齐。
---

## 摘要
符号回归 (SR) 旨在从数据中发现数学表达式，传统上这一任务通过遗传规划 (GP) 在符号结构上进行组合搜索来解决。潜空间优化 (LSO) 方法使用神经编码器将符号表达式映射到连续空间，从而将组合搜索转化为连续优化。SNIP (Meidani 等人, 2024) 是一种受 CLIP 启发的对比预训练模型，它通过引入多模态方法推进了 LSO：在共享潜空间中对齐符号编码器和数值编码器，以学习表型-基因型映射，从而使数值空间中的优化能够隐式引导符号搜索。然而，这依赖于细粒度的跨模态对齐，而关于 CLIP 等类似模型的文献表明，这种对齐通常是粗粒度的。在本文中，我们研究了 SNIP 是否实现了其对 SR 进行有效双模态优化的承诺。我们的实验表明：(1) 即使适应度增加，跨模态对齐在优化过程中也没有改善；(2) SNIP 学习到的对齐过于粗粒度，无法在符号空间中有效地进行有原则的搜索。这些发现表明，虽然多模态 LSO 在 SR 方面具有巨大潜力，但有效的对齐引导优化在实践中仍未实现，这突显了细粒度对齐是未来工作的一个关键方向。

## Abstract
Symbolic regression (SR) aims to discover mathematical expressions from data, a task traditionally tackled using Genetic Programming (GP) through combinatorial search over symbolic structures. Latent Space Optimization (LSO) methods use neural encoders to map symbolic expressions into continuous spaces, transforming the combinatorial search into continuous optimization. SNIP (Meidani et al., 2024), a contrastive pre-training model inspired by CLIP, advances LSO by introducing a multi-modal approach: aligning symbolic and numeric encoders in a shared latent space to learn the phenotype-genotype mapping, enabling optimization in the numeric space to implicitly guide symbolic search. However, this relies on fine-grained cross-modal alignment, whereas literature on similar models like CLIP reveals that such an alignment is typically coarse-grained. In this paper, we investigate whether SNIP delivers on its promise of effective bi-modal optimization for SR. Our experiments show that: (1) cross-modal alignment does not improve during optimization, even as fitness increases, and (2) the alignment learned by SNIP is too coarse to efficiently conduct principled search in the symbolic space. These findings reveal that while multi-modal LSO holds significant potential for SR, effective alignment-guided optimization remains unrealized in practice, highlighting fine-grained alignment as a critical direction for future work.

---

## 论文详细总结（自动生成）

这是一份关于论文《Multi-Modal Learning meets Genetic Programming: Analyzing Alignment in Latent Space Optimization》的结构化深入分析总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：符号回归（SR）传统上依赖遗传规划（GP）的组合搜索，而潜空间优化（LSO）通过神经编码器将这一过程转化为连续空间优化。SNIP 模型受 CLIP 启发，试图通过对比预训练对齐“符号结构”与“数值行为”，从而利用数值空间的优化来隐式引导符号搜索。
*   **核心问题**：论文质疑 SNIP 所依赖的跨模态对齐是否真的在优化中起到了作用。具体探讨两个假设：
    1.  LSO 算法在优化过程中是否真正利用了学习到的对齐信息？
    2.  这种对齐的粒度是否足够细，以至于能区分结构相似但语义不同的数学表达式？

### 2. 论文提出的方法论
*   **核心思想**：通过实证研究分析 SNIP 模型在符号回归任务中的对齐动态和辨别能力。
*   **关键技术细节**：
    *   **SNIP 架构**：包含两个 Transformer 编码器，分别处理符号表达式（前缀序列）和数值观测值（x, y 对）。
    *   **对比学习（InfoNCE Loss）**：通过最大化匹配对（同一方程的符号与数值）的余弦相似度，最小化不匹配对的相似度来学习共享潜空间。
    *   **LSO 流程**：在数值潜空间中进行连续优化（如灰狼优化算法 GWO），随后通过解码器将潜向量转回符号表达式。
*   **分析方法**：
    *   **研究一（对齐动态）**：在优化迭代中同时追踪适应度（$R^2$）和跨模态对齐度（余弦相似度）。
    *   **研究二（对齐粒度）**：设计检索任务，给定目标数值行为，要求模型从正确表达式及其微小扰动变体（如算子交换、常数改变）中识别出正确项。

### 3. 实验设计
*   **数据集**：
    *   **SRBench**：选取了 Feynman 和 Strogatz 物理方程基准测试集（变量维度 $D \le 10$）。
    *   **合成数据集**：100 个符合 SNIP 训练分布的随机生成表达式，作为性能上限参考。
*   **Benchmark 与对比方法**：
    *   以原始 SNIP 模型为核心分析对象。
    *   在检索任务中对比了**随机基准（Random Baseline）**。
*   **扰动策略**：包括一元/二元算子交换（如 $\sin \leftrightarrow \cos$）、常数微调、变量替换。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练总时长。
*   **实现细节**：作者提到复用了 SNIP 原始论文提供的预训练编码器和超参数设置，优化过程运行了 80 个迭代。

### 5. 实验数量与充分性
*   **实验规模**：
    *   研究一覆盖了 Feynman 和 Strogatz 全集。
    *   研究二使用了 82 个 Feynman 测试案例和 100 个合成案例，每个案例包含约 3.6 到 4.4 个候选变体。
*   **充分性评价**：实验设计较为充分且具有针对性。通过对不同扰动类型的“误导率”进行消融分析（如排除常数干扰），深入探讨了模型在不同符号组件上的辨别力。实验结果在物理基准和合成数据上表现一致，具有较强的客观性。

### 6. 论文的主要结论与发现
*   **对齐与优化脱节**：在 LSO 过程中，虽然数值适应度（$R^2$）显著提升，但跨模态对齐度保持平坦甚至下降。这表明优化过程主要依赖数值拟合，并未利用符号对齐信息。
*   **对齐粒度极粗**：SNIP 在 Feynman 数据集上的检索准确率仅为 18.3%，**低于随机猜测（27.3%）**。模型经常赋予错误的扰动变体更高的相似度。
*   **算子敏感度缺失**：模型最难区分二元算子的交换（误导率最高），而对变量替换相对敏感。这说明模型捕获的是宏观的函数趋势，而非精确的符号逻辑。

### 7. 优点
*   **批判性视角**：首次深入剖析了多模态 LSO 模型中“对齐引导”这一核心假设的真实性，揭示了性能提升背后的真实原因（主要是数值拟合而非符号理解）。
*   **评估框架创新**：借鉴视觉-语言模型的分析方法，为符号回归引入了“细粒度扰动检索”这一评估对齐质量的新范式。
*   **指明方向**：明确指出“细粒度对齐”是未来多模态符号回归研究的关键瓶颈。

### 8. 不足与局限
*   **模型覆盖面**：主要针对 SNIP 模型进行了深入分析，虽然 SNIP 具有代表性，但结论是否完全适用于其他基于扩散模型或不同损失函数的最新多模态 SR 模型仍需验证。
*   **缺乏改进方案**：论文重点在于“诊断”问题，虽然提出了未来方向（如硬负采样、结构化对比损失），但未在文中实现并验证这些改进方案的有效性。
*   **应用限制**：目前的发现主要集中在科学发现类的符号回归，对于更通用的程序合成等任务的泛化性讨论较少。

（完）
