---
title: Tensor-based computation of the Koopman generator via operator logarithm
title_zh: 基于算子对数的 Koopman 生成元张量计算
authors: "Tatsuya Kishimoto, Jun Ohkubo"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.07685v1"
tags: ["query:pde-discovery"]
score: 9.0
evidence: 利用Koopman生成器从数据中识别非线性动力系统的控制方程
tldr: 针对非线性动力系统识别中高维数据带来的“维度灾难”以及对采样频率要求高的问题，本文提出一种基于张量分解的Koopman算子生成器计算方法。该方法结合算子对数技术以避免数值微分，并利用低秩张量训练（TT）格式进行高效计算。通过在Lotka-Volterra和Lorenz-96系统上的实验，证明了该方法在保持高精度的同时，具有良好的高维扩展性，为复杂动力系统的建模提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.07685v1/fig-001.webp\", \"caption\": \"FIG. 5. Graphical representation of the reduced-matrix computation. The tensors UX,r and UY,r are left-orthonormalized so that the contractions can be performed efficiently. Since we define Ψ(Y) = KΨ(X), the tensor contraction sequence differs from that in the original algorithm.\", \"page\": 8, \"index\": 1, \"width\": 414, \"height\": 367}]"
motivation: 传统的系统识别方法如SINDy依赖数值微分且难以应对高维系统的计算复杂性与维度灾难问题。
method: 提出在保持低秩张量训练（TT）格式的前提下，通过对Koopman特征值取对数来直接计算Koopman生成器。
result: 在4维Lotka-Volterra和10维Lorenz-96系统实验中，该方法准确还原了向量场系数并展现出优异的扩展性。
conclusion: 该方法有效结合了算子对数与张量分解技术，为高维非线性系统提供了一种低成本且高精度的识别方案。
---

## 摘要
从数据中识别非线性动力系统的控制方程具有挑战性。虽然非线性动力学稀疏识别 (SINDy) 及其扩展被广泛用于系统辨识，但算子对数方法通过使用对数来避免时间微分，从而允许更大的采样间隔。然而，这些方法仍然受到维数灾难的影响。为此，我们提出了一种数据驱动的方法，通过在保持张量列 (TT) 格式的同时对 Koopman 特征值取对数，以低秩 TT 格式计算 Koopman 生成元。在 4 维 Lotka-Volterra 和 10 维 Lorenz-96 系统上的实验表明，该方法能够准确恢复向量场系数，并对更高维系统具有良好的可扩展性。

## Abstract
Identifying governing equations of nonlinear dynamical systems from data is challenging. While sparse identification of nonlinear dynamics (SINDy) and its extensions are widely used for system identification, operator-logarithm approaches use the logarithm to avoid time differentiation, enabling larger sampling intervals. However, they still suffer from the curse of dimensionality. Then, we propose a data-driven method to compute the Koopman generator in a low-rank tensor train (TT) format by taking logarithms of Koopman eigenvalues while preserving the TT format. Experiments on 4-dimensional Lotka-Volterra and 10-dimensional Lorenz-96 systems show accurate recovery of vector field coefficients and scalability to higher-dimensional systems.

---

## 论文详细总结（自动生成）

这是一份关于论文《Tensor-based computation of the Koopman generator via operator logarithm》的结构化深入分析总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：从观测数据中识别非线性动力系统的控制方程（系统辨识）是科学与工程领域的核心挑战。
*   **背景问题**：
    *   **微分敏感性**：传统方法如 SINDy 依赖于对时间导数的估计，这在噪声环境下或采样间隔较大时极不稳定。
    *   **维度灾难**：虽然基于 Koopman 算子的方法（如 EDMD）可以将非线性系统转化为线性问题，但随着系统维度的增加，基函数字典的大小呈指数级增长，导致计算和内存开销不可行。
*   **核心目标**：提出一种既能避免数值微分（利用算子对数技术），又能克服高维计算瓶颈（利用低秩张量分解）的新型系统辨识框架。

### 2. 论文提出的方法论
该方法的核心思想是在**张量列（Tensor Train, TT）**格式下，通过对 Koopman 算子的特征值取对数来间接获取其生成元（Generator）。

*   **核心思想**：
    1.  利用 Koopman 算子 $K_t$ 与其生成元 $L$ 之间的指数关系：$K_t = e^{tL}$。
    2.  通过特征分解 $K = P \Sigma P^{-1}$，将算子对数运算转化为对特征值的标量对数运算：$L = \frac{1}{T_s} P \log(\Sigma) P^{-1}$。
*   **关键技术细节**：
    *   **TT 格式表示**：将高维字典和算子表示为 TT 格式，将存储需求从 $O(n^d)$ 降低到 $O(d \cdot n \cdot r^2)$。
    *   **AMUSEt 算法集成**：利用 AMUSEt 算法直接在 TT 格式下计算 Koopman 算子的特征值和特征张量（Eigentensor），避免了显式构建巨大的全量矩阵。
    *   **生成元重构**：在 TT 格式下执行张量收缩（Tensor Contraction），通过特征对数重构生成元 $L$ 的 TT 核心，进而提取向量场系数。

### 3. 实验设计
论文通过两个具有代表性的动力系统验证了方法的有效性：
*   **数据集/场景**：
    1.  **4 维 Lotka-Volterra 系统**：用于验证系数恢复的精确度。
    2.  **10 维 Lorenz-96 系统**：用于测试高维扩展性和内存节省能力。
*   **Benchmark 与对比**：
    *   **对比对象**：传统的基于全量矩阵的算子对数方法（Matrix-logarithm approach）。
    *   **评估指标**：均方根误差 (RMSE)、归一化均方根误差 (NRMSE) 以及内存消耗（张量元素数量）。

### 4. 资源与算力
*   **软件环境**：使用 Python 语言，核心库包括 `scipy.integrate`（用于生成轨迹数据）和 `scipy.linalg`。
*   **硬件/算力说明**：论文**未明确指出**具体的 GPU 或 CPU 型号及训练时长。但文中强调了内存消耗的对比：在 10 维系统中，传统方法预计需要约 $3.49 \times 10^9$ 个元素，而本方法仅需 $4.37 \times 10^6$ 个元素，内存需求降低了约三个数量级。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对 Lotka-Volterra 系统，使用了 3 组不同初始条件的轨迹，每组 5000 个数据对。
    *   针对 Lorenz-96 系统，使用了 20000 个数据对。
*   **充分性评价**：实验设计较为**客观且具有针对性**。通过从低维到高维的过渡，清晰地展示了方法在精度保持和规模扩展上的优势。然而，实验主要集中在确定性系统上，未深入探讨高噪声环境下的表现。

### 6. 论文的主要结论与发现
*   **高精度恢复**：在 4 维系统中，NRMSE 达到了 $8.05 \times 10^{-5}$，证明了该方法能极高精度地还原向量场系数。
*   **突破维度限制**：成功处理了 10 维系统在 $3^{10}$ 维字典空间下的辨识问题，这在传统全量矩阵方法中几乎无法实现。
*   **内存效率**：TT 格式显著降低了存储开销，使得在普通计算设备上处理高维动力系统成为可能。

### 7. 优点（亮点）
*   **无需微分**：通过算子对数避开了数值微分带来的噪声放大问题，允许更大的采样间隔。
*   **结构保持**：首次提出了在 TT 格式下通过特征分解计算算子对数的方法，解决了张量格式下矩阵对数运算的难题。
*   **理论完备**：将 Koopman 算子理论、张量计算与系统辨识有机结合，具有较强的数学支撑。

### 8. 不足与局限
*   **噪声鲁棒性未知**：论文主要在干净的仿真数据上实验，实际工业数据中的噪声可能对特征分解的稳定性产生影响。
*   **稀疏性缺失**：目前的框架尚未集成类似 SINDy 的稀疏促进正则化（如 LASSO），在处理具有大量冗余项的字典时，可能无法自动筛选出最简化的物理方程。
*   **计算瓶颈**：虽然内存降低了，但 TT 格式下的特征分解（尤其是 AMUSEt 算法中的截断 SVD）在极高维度下仍存在计算耗时问题。

（完）
