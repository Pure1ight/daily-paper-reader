---
title: "AC-SINDy: Compositional Sparse Identification of Nonlinear Dynamics"
title_zh: AC-SINDy：非线性动力学的组合式稀疏识别
authors: Peter Racioppo
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18889v1"
tags: ["query:pde-discovery"]
score: 9.0
evidence: 用于非线性动力学稀疏识别的SINDy组合扩展方法
tldr: 针对传统SINDy方法在处理复杂非线性系统时面临的特征库组合爆炸问题，本文提出了AC-SINDy框架。该方法利用算术电路替代显式特征库，通过线性函数组合与乘法交互构建非线性特征，实现了紧凑且可扩展的参数化表示。同时，结合潜状态推理与多步监督机制，提高了对噪声的鲁棒性。实验证明，该方法能准确恢复混沌系统的控制方程，在可扩展性和解释性上显著优于传统SINDy。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.18889v1/fig-001.webp\", \"caption\": \"Figure 4: Left: Predicted vs. true trajectory for a single state variable. Right: Phase-space trajectory. The learned dynamics capture the qualitative structure despite long-term divergence.\", \"page\": 9, \"index\": 1, \"width\": 802, \"height\": 390}, {\"url\": \"assets/figures/arxiv/2604.18889v1/fig-002.webp\", \"caption\": \"Figure 3: Training loss during iterative pruning. Loss spikes occur when parameters are removed, followed by recovery as the model re-optimizes. Excessive pruning degrades performance.\", \"page\": 9, \"index\": 2, \"width\": 498, \"height\": 483}, {\"url\": \"assets/figures/arxiv/2604.18889v1/fig-003.webp\", \"caption\": \"Figure 2: Parameter growth for standard SINDy (explicit enumeration) versus AC-SINDy (compositional representation) for polynomial orders p = 2, 3, 4.\", \"page\": 6, \"index\": 3, \"width\": 826, \"height\": 260}, {\"url\": \"assets/figures/arxiv/2604.18889v1/fig-004.webp\", \"caption\": \"Figure 1: Illustration of SINDy regression showing sparse identification over a candidate library, adapted from Brunton et al. [2016].\", \"page\": 4, \"index\": 4, \"width\": 664, \"height\": 379}, {\"url\": \"assets/figures/arxiv/2604.18889v1/fig-005.webp\", \"caption\": \"Figure 5: Noisy system identification with Gaussian noise (σ = 0.05). The model fits the noisy observations while recovering a smooth approximation of the underlying dynamics.\", \"page\": 10, \"index\": 5, \"width\": 582, \"height\": 439}]"
motivation: 解决传统SINDy在构建非线性特征库时因候选函数枚举导致的维度灾难和扩展性差的问题。
method: 采用算术电路结构化表示动力学特征，并结合潜状态推理与多步监督学习来分离状态估计与动力学识别。
result: 在非线性和混沌系统实验中，该方法能够以更优的扩展性准确恢复出具有高度可解释性的控制方程。
conclusion: AC-SINDy通过组合式参数化显著提升了稀疏动力学识别的效率与鲁棒性，为复杂系统建模提供了更高效的工具。
---

## 摘要
我们提出了 AC-SINDy，这是非线性动力学稀疏识别（SINDy）框架的一种组合式扩展，它使用基于算术电路的结构化表示取代了显式特征库。该方法并非枚举候选基函数，而是通过线性函数的组合和乘法交互来构建非线性特征，从而实现紧凑且可扩展的参数化，并允许直接在计算图上实施稀疏性。我们还引入了一种将状态估计与动力学识别分离的方案，通过结合潜状态推断、共享动力学和多步监督，在保持可解释性的同时提高了对噪声的鲁棒性。在非线性和混沌系统上的实验表明，该方法能够恢复出准确且可解释的控制方程，同时比标准 SINDy 具有更好的扩展性。

## Abstract
We present AC-SINDy, a compositional extension of the Sparse Identification of Nonlinear Dynamics (SINDy) framework that replaces explicit feature libraries with a structured representation based on arithmetic circuits. Rather than enumerating candidate basis functions, the proposed approach constructs nonlinear features through compositions of linear functions and multiplicative interactions, yielding a compact and scalable parameterization and enabling sparsity to be enforced directly over the computational graph. We also introduce a formulation that separates state estimation from dynamics identification by combining latent state inference with shared dynamics and multi-step supervision, improving robustness to noise while preserving interpretability. Experiments on nonlinear and chaotic systems demonstrate that the method recovers accurate and interpretable governing equations while scaling more favorably than standard SINDy.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **AC-SINDy** 的新框架，旨在改进经典的非线性动力学稀疏识别（SINDy）方法。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的 SINDy 方法依赖于预定义的候选函数库（如多项式、三角函数）。当系统维度增加或非线性阶数提高时，候选库的大小会呈**组合爆炸式增长**（维度灾难），导致计算成本剧增且难以处理复杂交互。
*   **研究背景**：许多物理系统具有稀疏且低阶的交互结构，但 SINDy 通过枚举所有可能项来捕捉这些结构的做法效率低下。此外，在噪声数据中准确估计导数并保持模型的可解释性也是一个长期挑战。

### 2. 方法论：核心思想与技术细节
AC-SINDy 的核心是将“显式特征库”替换为“结构化计算图（算术电路）”。
*   **算术电路（Arithmetic Circuits）架构**：
    *   **线性层（求和节点）**：对输入状态进行加权组合。
    *   **交互层（乘积节点）**：通过对线性层的输出进行乘法运算来构建高阶非线性特征。
    *   **函数表示**：模型将动力学函数表示为因式分解的形式（类似于张量的 CP 分解），这使得参数量随维度的增长从指数级降低为线性或二次级。
*   **基于梯度的重要性剪枝**：
    *   不同于 SINDy 的权重绝对值阈值化，AC-SINDy 使用一阶泰勒展开近似参数对损失函数的影响：$\Delta_i \approx w_i \frac{\partial L}{\partial w_i}$。
    *   通过迭代剪枝掉重要性低的边，最终在计算图中留下稀疏且可解释的结构。
*   **特征归一化（Feature Normalization）**：引入尺度不变的参数化方案，防止乘法交互导致的特征尺度剧烈波动，确保学习到的系数能真实反映功能重要性。
*   **状态估计与动力学分离**：
    *   结合潜状态推理（Encoder）与多步预测监督（Multi-step Consistency）。
    *   通过要求模型在多个时间步长内保持预测一致性，迫使模型学习去噪后的状态表示，从而提高对噪声的鲁棒性。

### 3. 实验设计
*   **实验场景**：
    1.  **2D 非线性系统**：包含二次项和线性项的简单动力学。
    2.  **Lorenz 系统**：经典的 3D 混沌系统。
    3.  **带正弦驱动的 Lorenz 系统**：增加随时间变化的外部强迫项。
    4.  **噪声环境实验**：在 2D 系统中加入高斯噪声（$\sigma=0.05$）测试去噪识别能力。
*   **基准对比（Benchmark）**：主要对比对象是**标准 SINDy**。论文通过理论分析和实验展示了在参数增长曲线（Parameter Growth）上 AC-SINDy 的优势。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量或训练时长。
*   **实现细节**：提到使用 PyTorch 实现，并提供了 GitHub 开源地址。考虑到处理的是低维动力学系统（2D 和 3D），预计该方法在普通消费级 GPU 或 CPU 上即可运行。

### 5. 实验数量与充分性
*   **实验规模**：论文展示了 3 个典型动力学系统的恢复结果，并进行了针对性的噪声鲁棒性实验和参数扩展性分析（图 2 展示了不同阶数下的参数量对比）。
*   **充分性评价**：
    *   **优点**：涵盖了从简单非线性到复杂混沌系统的跨度，验证了核心假设（组合式表示优于枚举式）。
    *   **不足**：实验目前局限于低维系统（$n \le 3$）。虽然理论上证明了扩展性，但尚未在大规模高维数据集（如流体力学数据）上进行实证。

### 6. 主要结论与发现
*   **参数效率**：AC-SINDy 的参数量随维度 $d$ 和阶数 $p$ 的增长远慢于标准 SINDy（例如在 $p=4$ 时，SINDy 呈指数增长，而 AC-SINDy 保持二次增长）。
*   **方程恢复**：该方法能准确识别出 2D 系统和 Lorenz 系统的控制方程结构，即使在存在噪声的情况下也能捕捉到主要的线性与双线性项。
*   **鲁棒性**：多步一致性损失有效地分离了噪声和真实的动力学信号。

### 7. 优点
*   **高效的假设空间**：通过算术电路捕捉“低秩多项式结构”，比枚举所有单项式更符合物理直觉且更节省参数。
*   **结构化稀疏**：剪枝直接作用于计算图的边，产生的模型可以直接转化为符号表达式，保留了 SINDy 的核心可解释性。
*   **尺度不变性**：特征归一化解决了深度组合模型中常见的系数非识别性问题。

### 8. 不足与局限
*   **系数精度**：在 Lorenz 系统实验中，虽然识别出了正确的项，但部分系数与真实值存在偏差，且会出现少量冗余项（Spurious terms）。
*   **高维验证缺失**：尚未在真正的高维系统（如偏微分方程 PDE 离散化后的系统）中验证其性能。
*   **超参数敏感性**：迭代剪枝的频率、阈值以及多步预测的步数 $K$ 可能需要针对不同系统进行精细调优。

（完）
